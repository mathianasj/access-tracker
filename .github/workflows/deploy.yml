name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # TODO: Full implementation in M5 - helm upgrade on main merge
      - run: echo "Skeleton - full implementation in M5"
