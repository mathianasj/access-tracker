apiVersion: v2
name: access-tracker
description: A Helm chart for Access Tracker application
type: application
version: 0.1.0
appVersion: "0.1.0"

dependencies:
  - name: postgresql
    version: "*"
    repository: "https://charts.bitnami.com/bitnami"
    condition: postgresql.enabled
